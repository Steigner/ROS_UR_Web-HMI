[tool.poetry]
name = "ur_web_control"
version = "0.1.0"
description = ""
authors = ["Steigner"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
Flask = "^2.2.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
